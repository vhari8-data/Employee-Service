apiVersion: v1
kind: Service
metadata:
  name: springboot-service
spec:
  type: NodePort
  selector:
    app: springboot-app
  ports:
    - protocol: TCP
      port: 9292        # Service port
      targetPort: 9292  # Container port
      nodePort: 30080   # External port (optional, between 30000–32767)

